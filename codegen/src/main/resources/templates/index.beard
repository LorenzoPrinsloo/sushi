import akka.http.scaladsl.server.Directives.{as, concat, entity, pathPrefix, _}
import akka.http.scaladsl.server.Route

object {{ className }}Router {

    lazy val {{ className | lowercase }}Router: Route =
    pathPrefix("{{ className | lowercase }}") {
        concat(
            post {
                entity(as[{{ className }}]) { {{ className | lowercase }} =>
                    {{ className }}DAO.insert({{ className | lowercase }})
                }
            }
        )
    }
}
